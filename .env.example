# ================ Agent Development Stack Environment Configuration ===================
# Copy to `.env` (which is git-ignored) and customize values.
# This configuration works for both docker-compose services and external clients.
# ======================================================================================

# ====================== Global / Ingestion Observability =======================
NODE_ENV=development
LOG_LEVEL=info                 # trace|debug|info|warn|error
METRICS_PORT=9100              # Prometheus metrics -> localhost:9100/metrics
TLS_CERT_PASSWORD=changeit     # Used by ssl-cert-init for all certificate generation

# ============================= Neo4j ================================
# Connection URIs (TLS required)
NEO4J_BOLT_INTERNAL=neo4j+ssc://neo4j:7687
NEO4J_BOLT_EXTERNAL=neo4j+ssc://localhost:7687

# Auth
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme

# Memory (explicit units)
NEO4J_HEAP_INIT=3g
NEO4J_HEAP_MAX=7g

# ============================= Kafka ================================
# Ports / Brokers
KAFKA_LISTENER_PORT=19094
KAFKA_GROUP_ID=normalizer-group
KAFKA_BROKERS_INTERNAL=kafka:9094
KAFKA_BROKERS_EXTERNAL=localhost:19094

# Auth / SASL
KAFKA_USERNAME=kafka
KAFKA_PASSWORD=changeme
KAFKA_SASL_MECHANISM=SCRAM-SHA-256

# Topics
TOPIC_RAW=tasks.raw-output
TOPIC_DOMAIN=events.domain
TOPIC_DLQ=events.dlq
BATCH_FLUSH_MS=2000

# ============================== Qdrant ===============================
QDRANT_URL_INTERNAL=https://qdrant:6333
QDRANT_URL_EXTERNAL=https://localhost:6333
QDRANT_API_KEY=changeme

# ============================== MinIO ================================
MINIO_URL_INTERNAL=https://minio:9000
MINIO_URL_EXTERNAL=https://localhost:9000
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=changeme

# ============================== Plugins =================================
# Add any additional plugin-specific environment variables here.
#
# AI_API_KEY=your-api-key
# MCP_AUTH=your-api-key
# CLOUD_AUTH=your-api-key
# ...